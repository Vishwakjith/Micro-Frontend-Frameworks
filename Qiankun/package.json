{
  "dependencies": {
    "qiankun": "^2.10.16"
  },
  "type": "module",
  "scripts": {
    "install:all": "cd host && npm install && cd ../remote && npm install",
    "start:host": "cd host && npm start",
    "start:remote": "cd remote && npm start"
  }
}
